@model PaymentGateway21052021.Areas.SysSetup.Models.SysTransactionLimitViewModel.HoldDisplayTransactionLimitViewModel

@{
    ViewData["Title"] = "Transaction Limits";
    Layout = "~/Areas/Dashboard/Views/Shared/_Layout.cshtml";
}
<div class="card-body col-6">

    <h2 class="mb-2">
        Transaction Limit(s)
    </h2>

    <div class="card">
        <div class="card-body">

            <div class="col-10 mb-6">

                <form asp-action="TransactionLimitList" asp-controller="SysTransactionLimit" asp-area="SysSetup" method="get">
                    <div class="float-left mr-3">
                        <input name="searchText" placeholder="Enter Transaction Limit Name" class="form-control" />
                    </div>
                    <div class="float-left">
                        <input type="submit" value="Search" class="btn btn-info float-left mr-3" />
                        <a asp-action="TransactionLimit" class="btn btn-warning float-left ml-3">Create New Transaction Limit</a>
                    </div>

                </form>

            </div>


            @if (Model != null && Model.HoldAllTransactionLimits.Count() > 0)
            {

                <table class="table">
                    <thead>
                        <tr>
                            @*<th>S/N</th>*@
                            <th>
                                Transaction Limit Name
                            </th>
                            <th>
                                Transaction Limit
                            </th>
                            <th>
                                Transaction Limit Balance
                            </th>
                            <th>
                                Date Created
                            </th>
                            <th>
                                Action
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.HoldAllTransactionLimits)
                        {
                            //indx++;
                            <tr>
                                @*<td>@indx</td>*@
                                <td>
                                    @Html.DisplayFor(modelItem => item.TransactionLimitName)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TransactionLimit)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.TransactionBalance)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.DateCreated)
                                </td>
                                <td>
                                    <a asp-action="UpdateTransactionLimit" asp-route-id="@item.TransactionLimitID" class="btn btn-success float-left ml-3">
                                        <span class="fe fe-edit">Edit</span>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>

                <!-- pager -->
                @if (Model.Pager.EndPage > 1)
                {

                    <ul class="pagination">
                        @if (Model.Pager.CurrentPage > 1)
                        {
                            <li class="page-item">
                                <a href="~/SysSetup/SysTransactionLimit/TransactionLimitList" class="page-link">First</a>
                            </li>
                            <li class="page-item">
                                <a href="~/SysSetup/SysTransactionLimit/TransactionLimitList?page=@(Model.Pager.CurrentPage - 1)" class="page-link">Previous</a>
                            </li>
                        }

                        @for (var pageIndex = Model.Pager.StartPage; pageIndex <= Model.Pager.EndPage; pageIndex++)
                        {
                            var myPage = pageIndex;
                            <li class="@(pageIndex == Model.Pager.CurrentPage ? "active" : "") page-item">
                                <a href="~/SysSetup/SysTransactionLimit/TransactionLimitList?pageIndex=@myPage" class="page-link"> @myPage </a>
                            </li>
                        }

                        @if (Model.Pager.CurrentPage < Model.Pager.TotalPages)
                        {
                            <li class="page-item">
                                <a href="~/SysSetup/SysTransactionLimit/TransactionLimitList?pageIndex=@(Model.Pager.CurrentPage + 1)" class="page-link">Next</a>
                            </li>
                            <li class="page-item">
                                <a href="~/SysSetup/SysTransactionLimit/TransactionLimitList?pageIndex=@(Model.Pager.TotalPages)" class="page-link">Last</a>
                            </li>
                        }
                    </ul>
                }
            }
            else
            {
                <h2>No Available Transaction Limit(s)</h2>
            }
        </div>
    </div>
</div>